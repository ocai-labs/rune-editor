F1 second lesson — marks are grouped into runs before wrapping, widest run
first, so a mark shared by several runs becomes their common wrapper. Wrapping
each run on its own produced abutting delimiters that remark then escaped, and
the escape count doubled on every subsequent save.

**a** followed immediately by **b** in one sentence: **a****b** was the shape.

*You *and ***can*** share an italic run whose boundaries differ from the bold's.

Nested and partially overlapping: **bold with *inner italic* still bold** and
*italic with **inner bold** still italic*.

Three marks over adjacent runs: ***~~all three~~*** then **just bold**.
